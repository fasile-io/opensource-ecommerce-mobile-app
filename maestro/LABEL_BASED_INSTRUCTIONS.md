# Maestro Label-Based Instructions

Use label-based selectors in Maestro flows.

- Do not use `point:` selectors.
- Prefer visible text and accessibility labels for navigation and actions.
- Keep flow steps readable so labels are easy to maintain when the UI changes.
- Apply this rule to attached Android flows, including `maestro/flows/android/full_app_flow_attached.yaml`.
